/**
 * Pricing - 訂閱方案頁面
 * 與 Subscription.tsx 功能相同，但作為獨立的路由頁面
 */

import Subscription from './Subscription';

// Pricing 頁面直接使用 Subscription 組件
export default function Pricing() {
  return <Subscription />;
}
